# Optional. If unset, the two narrow LLM-reasoning steps (intake note
# parsing, human-readable approval summary) fall back to deterministic
# templates instead of calling the Claude API.
ANTHROPIC_API_KEY=
